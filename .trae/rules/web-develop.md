 内容聚焦规则：生成的检索代码、关键词、注释需贴合智慧农业、农学、生物育种、作物基因组学方向；优先适配农业/生物领域文献检索逻辑，兼顾计算机交叉学科文献。
 数据源适配规则：
调用PubMed API时，重点匹配生物/农学领域的文献检索规则；
调用OpenAlex API时，需额外处理「被引数据、引用关系」字段，为后续阶段3/5做铺垫；
 合并数据时，统一字段格式（如DOI标准化、时间格式统一），确保去重、排序逻辑准确。
 迭代约束规则：
阶段1-2完成前，禁止开发AI助手、可视化图谱等进阶功能；
公网部署（Streamlit Community Cloud）前，必须完成本地全联调，修复所有Bug（卡顿、无结果、乱码）。